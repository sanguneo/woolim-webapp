import { css } from '@emotion/react';

const title22 = css`
  font-size: 22px;
  word-spacing: -0.02em;
  color: var(--gray-900);
  font-weight: 800;
  line-height: 28px;
`;

const title20 = css`
  font-size: 20px;
  word-spacing: -0.02em;
  color: var(--gray-900);
  font-weight: 700;
  line-height: 25px;
`;

const title18 = css`
  font-size: 18px;
  word-spacing: -0.02em;
  color: var(--gray-900);
  font-weight: 600;
  line-height: 23px;
`;

const title16 = css`
  font-size: 16px;
  word-spacing: -0.01em;
  color: var(--gray-900);
  font-weight: 600;
  line-height: 21px;
`;

const title15 = css`
  font-size: 15px;
  word-spacing: -0.01em;
  color: var(--gray-900);
  font-weight: 500;
  line-height: 20px;
`;

export const Font = {
  title15,
  title16,
  title18,
  title20,
  title22,
};
